# include "head.h"
int ikaze()
{
		int amahitamo;

		printf("\n\t\t\tWell done then here is the menu :\n\t--------------------------------------------------------\n\tPress	1 -> to get the content of the list\n\tPress   2 -> to get the number of the nodes in the list\n\tPrees   3 -> to insert the node at the beginig of the list\n\tPress   4 -> to insert node at the end of the list\n\tPress   5 -> to remove a node from the head of the list\n\tPress   6-> to delete the last node\n\tPress   7 -> to delete a specific node\n\tPress   200 -> to exit the menu\n\t.So what did you choose ------------------- >\t:");
		scanf("%d",&amahitamo);
		return (amahitamo);
}
